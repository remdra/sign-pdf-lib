export interface Rectangle {
    left: number;
    top: number;
    right: number;
    bottom: number;
}

export const emptyRectangle: Rectangle = {
    left: 0,
    top: 0,
    right: 0,
    bottom: 0
}
